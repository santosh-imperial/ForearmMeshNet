# Install the package and all dependencies via pyproject.toml (recommended):
#   pip install -e .                  # core deps
#   pip install -e ".[dev]"           # + pytest / pytest-cov
#   pip install -e ".[dev,smplx]"     # + smplx for template generation from SMPL-X model

# Direct dependency list (mirrors pyproject.toml):
numpy>=1.23
scipy>=1.10
scikit-image>=0.22
trimesh>=4.0
scikit-learn
torch>=2.1
torch-geometric
pymeshfix>=0.16
matplotlib>=3.8
tqdm>=4.66
pyyaml>=6.0
rich>=13.7
nibabel>=5.0
meshio>=5.3
pandas>=2.0
open3d>=0.17
SimpleITK>=2.2
